* Using Git Bash "cd" command go inside of folder and type
```    
touch .gitkeep
```
* In .gitignore file enter it
```
/folder/*
!/folder/.gitkeep
```

*Note: Git does not track empty folder. Git only track file.*